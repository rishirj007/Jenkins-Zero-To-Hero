pipeline {
    agent any

    stages {
        stage('Build Java') {
            steps {
                dir('multi-stage-multi-agent/backend') {
                    sh 'mvn clean compile'
                }
            }
        }

        stage('Run Java') {
            steps {
                dir('multi-stage-multi-agent/backend') {
                    sh 'mvn exec:java'
                }
            }
        }

        stage('Install Node.js Packages') {
            steps {
                dir('multi-stage-multi-agent/frontend') {
                    sh 'npm install'
                }
            }
        }

        stage('Run Node.js App') {
            steps {
                dir('multi-stage-multi-agent/frontend') {
                    sh 'npm start'
                }
            }
        }
    }
}
